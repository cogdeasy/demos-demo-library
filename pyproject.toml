[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-library"
version = "0.2.0"
description = "Demo calculation library with toggleable features"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7.0"]
